<Page x:Class="ReadWriteDontRush.Pages.AuthorBooksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReadWriteDontRush.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuthorBooksPage" Background="White">

    <Page.Resources>
        <Style x:Key="BookCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="15" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="TxtTitle" FontSize="20" FontWeight="Bold"/>
                <TextBox x:Name="TxtSearch" Margin="0,10,0,0" Height="35" 
                         TextChanged="TxtSearch_TextChanged"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="BooksItemsControl" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="220" Style="{StaticResource BookCardStyle}">
                            <StackPanel>
                                <Border Height="280" Background="#ECF0F1" CornerRadius="8,8,0,0">
                                    <Image Width="180" Height="250" Margin="10">
                                        <Image.Source>
                                            <Binding Path="CoverImagePath"/>
                                        </Image.Source>
                                    </Image>
                                </Border>

                                <StackPanel Margin="10">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" 
                                               FontSize="14" TextWrapping="Wrap"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                        <TextBlock Text="★" Foreground="Gold" FontSize="14"/>
                                        <TextBlock Text="{Binding AverageRating, StringFormat={}{0:F1}}" 
                                                   FontSize="12" Margin="5,0,0,0"/>
                                    </StackPanel>

                                    <TextBlock Text="{Binding GenresString}" FontSize="10" 
                                               Foreground="#7F8C8D" TextWrapping="Wrap"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button Content="Редактировать" Width="100" Height="30"
                                                Background="#3498DB" Foreground="White"
                                                Click="BtnEdit_Click" Tag="{Binding}"/>
                                        <Button x:Name="BtnAppeal" Content="Оспорить" Width="90" Height="30"
                                                Margin="5,0,0,0" Background="#E67E22" Foreground="White"
                                                Visibility="{Binding ShowAppealButton}"
                                                Click="BtnAppeal_Click" Tag="{Binding}"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
