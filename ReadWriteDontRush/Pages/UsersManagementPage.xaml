<Page x:Class="ReadWriteDontRush.Pages.UsersManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReadWriteDontRush.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Управление пользователями" Background="#F5F5F5">

    <Page.Resources>
        <DropShadowEffect x:Key="ShadowEffect" BlurRadius="5" ShadowDepth="2" Opacity="0.15"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="👥 Управление пользователями" FontSize="22" FontWeight="Bold" 
                   Margin="0,0,0,20" Foreground="#2C3E50"/>

        <!-- Поиск -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <TextBox x:Name="TxtSearch" Height="35" TextChanged="TxtSearch_TextChanged"/>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="UsersItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" CornerRadius="8" Padding="15" 
                                Margin="0,0,0,10" Effect="{StaticResource ShadowEffect}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" FontWeight="Bold" FontSize="16">
                                    <Run Text="{Binding DisplayName}"/>
                                    <Run Text=" (" Foreground="Gray"/>
                                    <Run Text="{Binding Login}" Foreground="Gray"/>
                                    <Run Text=")"/>
                                </TextBlock>

                                <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Email}" 
                                           FontSize="12" Foreground="Gray" Margin="0,5,0,5"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" FontSize="12">
                                    <Run Text="Роль: " FontWeight="Bold"/>
                                    <Run Text="{Binding RoleName}"/>
                                </TextBlock>

                                <TextBlock Grid.Row="3" Grid.Column="0" FontSize="12">
                                    <Run Text="Статус: " FontWeight="Bold"/>
                                    <Run Text="{Binding Status}" Foreground="{Binding StatusColor}"/>
                                </TextBlock>

                                <TextBlock Grid.Row="2" Grid.Column="1" FontSize="12">
                                    <Run Text="Дата регистрации: " FontWeight="Bold"/>
                                    <Run Text="{Binding CreatedAt, StringFormat='dd.MM.yyyy'}"/>
                                </TextBlock>

                                <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" 
                                            Margin="0,10,0,0" HorizontalAlignment="Right">
                                    <ComboBox x:Name="CmbChangeRole" Width="150" Height="30"
                                              SelectionChanged="CmbChangeRole_SelectionChanged" Margin="0,0,10,0">
                                        <ComboBoxItem Content="Пользователь" Tag="2"/>
                                        <ComboBoxItem Content="Автор" Tag="3"/>
                                        <ComboBoxItem Content="Администратор" Tag="1"/>
                                    </ComboBox>
                                    <Button Content="Сменить пароль" Width="120" Height="30"
                                            Background="#3498DB" Foreground="White"
                                            Click="ChangePassword_Click" Tag="{Binding}"/>
                                    <Button x:Name="BtnFreezeUser" Content="Заморозить" Width="100" Height="30"
                                            Margin="5,0,0,0" Background="#E74C3C" Foreground="White"
                                            Click="FreezeUser_Click" Tag="{Binding}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
