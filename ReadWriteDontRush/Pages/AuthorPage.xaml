<Page x:Class="ReadWriteDontRush.Pages.AuthorPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReadWriteDontRush.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AuthorPage" Background="White">

    <Page.Resources>
        <Style x:Key="BookCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <TextBlock Text="Страница автора" FontSize="24" FontWeight="Bold" 
                       Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <Border Grid.Row="1" Background="White" Padding="15" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                    <Button x:Name="BtnMyBooks" Content="📚 Мои книги" Width="150" Height="40" 
                            Background="#3498DB" Foreground="White" Margin="5"
                            Click="BtnMyBooks_Click"/>
                    <Button x:Name="BtnAddBook" Content="➕ Добавить книгу" Width="150" Height="40" 
                            Background="#27AE60" Foreground="White" Margin="5"
                            Click="BtnAddBook_Click"/>
                    <Button x:Name="BtnFrozenBooks" Content="❄️ Замороженные книги" Width="180" Height="40" 
                            Background="#E74C3C" Foreground="White" Margin="5"
                            Click="BtnFrozenBooks_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="BooksItemsControl" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="220" Style="{StaticResource BookCardStyle}">
                            <StackPanel>
                                <Border Height="280" Background="#ECF0F1" CornerRadius="8,8,0,0">
                                    <Image Width="200" Height="260" Margin="10,10,10,10"
                                           Stretch="UniformToFill">
                                        <Image.Source>
                                            <Binding Path="CoverImagePath">

                                            </Binding>
                                        </Image.Source>
                                    </Image>
                                </Border>

                                <StackPanel Margin="10">
                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" 
                                               FontSize="14" TextWrapping="Wrap"
                                               Margin="0,0,0,5"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="★" Foreground="Gold" FontSize="14"/>
                                        <TextBlock Text="{Binding AverageRating, StringFormat={}{0:F1}}" 
                                                   FontSize="12" Margin="5,0,0,0"/>
                                        <TextBlock Text="{Binding ReviewsCount, StringFormat='(отзывов: {0})'}" 
                                                   FontSize="10" Foreground="Gray" Margin="5,0,0,0"/>
                                    </StackPanel>

                                    <TextBlock Text="{Binding GenresString}" FontSize="10" 
                                               Foreground="#7F8C8D" TextWrapping="Wrap"
                                               MaxHeight="40"/>

                                    <TextBlock Text="{Binding CreatedAt, StringFormat='Дата: {0:dd.MM.yyyy}'}" 
                                               FontSize="10" Foreground="Gray" Margin="0,5,0,5"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button Content="Редактировать" Width="100" Height="30"
                                                Background="#F39C12" Foreground="White"
                                                Click="BtnEditBook_Click" Tag="{Binding}"
                                                Visibility="{Binding ShowEditButton}"/>
                                        <Button x:Name="BtnAppealFreeze" Content="Оспорить заморозку" Width="120" Height="30"
                                                Margin="5,0,0,0" Background="#E67E22" Foreground="White" 
                                                Click="BtnAppealFreeze_Click" Tag="{Binding BookID}"
                                                Visibility="{Binding ShowAppealButton}"/>
                                        <TextBlock x:Name="TxtAppealSent" Text="✓ Заявка отправлена" 
                                                   FontSize="11" Foreground="Green" Margin="10,8,0,0"
                                                   Visibility="{Binding ShowAppealSentText}"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock x:Name="TxtEmptyMessage" Grid.Row="2" 
                   Text="У вас пока нет книг" 
                   FontSize="16" Foreground="Gray"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
